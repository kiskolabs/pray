[package]
name = "pray-bench"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
pray-core = { path = "../pray-core" }
tempfile = "3"

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "operations"
harness = false
