# Prepare 1.9.0 release

## Participants

Andrei Makarov

## Decisions

Cut 1.9.0 on main after the RFC contract move and fail-only conflict parse. Keep 2.0.0 for removing deprecated target, output, and agent. Leave Dependabot PRs 10, 12, and 13 open. Stale local dependabot branches stay unmerged.

## Effects

Local main holds five commits ahead of origin/main: retire SPEC.md, fail-only parse, VCS README, version 1.9.0, rfc_ids scratch isolation. Version files read 1.9.0. make release-dry-run finished: pray-core package dry-run uploaded abort, pray-transport and pray-cli dry-run fell back to cargo check because crates.io still has pray-core 1.8.1, npm pack and Ruby gem build produced pray-cli 1.9.0. cargo test -p pray-core --test rfc_ids passed three times after the isolation fix. No tag, no GitHub Release, no registry publish.

## Next

Push origin main. Tag v1.9.0. Create a GitHub Release so pray upgrade notices resolve. Publish in order: scripts/release/crates.sh --publish, then npm.sh --publish, then rubygems.sh --publish. Optional make bump-homebrew after the tag exists.

## Source

CHANGELOG.md 1.9.0. docs/releasing.md. scripts/release/README.md.
