# Ruby pray format recommended rewrite

## Participants

- Andrei Makarov

## Decisions

- Port Rust and TypeScript pray format recommend/serialize into Ruby so format and fmt rewrite legacy target blocks to compose, tree, and file: destinations with the same round-trip check.
- Keep marker-comment cleanup on locked outputs after the Prayfile rewrite, matching the Rust CLI sequence.

## Effects

- Ruby gem gains FormatManifest and FormatSerialize; CLI format and fmt resolve offline-first, rewrite Prayfile when needed, then canonicalize pray markers in existing outputs.
- Specs cover legacy migration, hybrid file: plus legacy targets, source keyword omission, idempotent DSL formatting, and hint-driven role binding.

## Next

- Grow the shared corpus when further destination or format edge cases need cross-runtime lock-in.

## Source

- rubygems/pray-cli/lib/pray/format_manifest.rb
- rubygems/pray-cli/lib/pray/format_serialize.rb
- rubygems/pray-cli/lib/pray/cli/commands/workflow.rb
- rubygems/pray-cli/spec/pray/format_manifest_spec.rb
- crates/pray-core/src/format_manifest.rs
- npmjs/pray-cli/src/manifest/format-manifest.ts
