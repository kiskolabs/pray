# Initial Rust reference CLI

Build the first executable slice of the `pray` reference implementation:

- parse `Prayfile`
- parse `*.prayspec`
- resolve local path packages
- write `Prayfile.lock`
- render managed `INSTRUCTIONS.md`
- verify managed spans

This is the first implementation pass toward the spec-defined workflow.

